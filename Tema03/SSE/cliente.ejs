<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>HORA ACTUALIZADA DESDE EL SERVIDOR:</h1>
    <div id="time"></div>
    <div id="number"></div>

    <script>
        const eventSource=new EventSource('http://127.0.0.1:3000/events');
        eventSource.onmessage=(event)=>{
            const datos=event.data.split("-");
            document.getElementById('time').textContent=datos[0];
            document.getElementById('number').textContent=datos[1];
                
            
         
        }

        eventSource.onerror=function(event){
            console.error('Error de SEE: ',event);
            eventSource.close();
        }
    </script>
</body>
</html>